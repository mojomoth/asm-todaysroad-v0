# 디자인 유사도 체크리스트 — v0.jpg 대비 (측정 기준)

> 디자인 가드레일 "v0.jpg와 유사도 ≥80%"를 반증 가능하게 만들기 위한 요소별 가중 체크리스트.
> 라운드1 devil R1-7("측정 도구 부재로 주관 주장") 해소용. 시각 회귀 자동화(pixelmatch/Playwright)는 V1.
> 각 요소 0~1 충족도 × 가중치. 평가 기준일: 라운드2 종료 시점.

| # | 레퍼런스 요소 (docs/DESIGN.md) | 가중 | 충족 | 근거 (구현 위치) |
|---|---|---:|---:|---|
| 1 | 풀스크린 밝은 지도(배경) | 12 | 1.0 | `useKakaoMap` + `.tr-map` 밝은 톤. 실패 시 파스텔 fallback |
| 2 | 상단 흰 pill 플로팅 헤더 | 10 | 1.0 | `.tr-header` 흰 pill + blur + 약한 그림자 |
| 3 | 헤더 좌측 로고마크 | 6 | 1.0 | `FloatingHeader` `LogoMark` |
| 4 | 헤더 우측 카메라 + 브랜드 아이콘 | 6 | 1.0 | `CameraIcon` + `BrandIcon` |
| 5 | "텍스트 없이 아이콘 중심" | 6 | 1.0 | 라운드2에서 헤더 "오늘의길" 텍스트 제거 |
| 6 | 코랄 레드 추천 핀 + 중앙 흰 원 | 10 | 1.0 | `markerContent` `pinSvg(CORAL)` |
| 7 | 파란 현위치 마커(teardrop + 흰 원 + 그림자) | 8 | 1.0 | 라운드2에서 닷→파란 teardrop 핀 + `__shadow` |
| 8 | 하단 흰 카드(큰 둥근모서리·부드러운 그림자) | 12 | 1.0 | `.tr-card` radius-lg + shadow-card |
| 9 | 좌측 원형 썸네일 | 8 | 0.6 | `.tr-card__thumb` 원형이나 사진 아닌 단색+이모지(V1 실이미지) |
| 10 | 우측 코스 제목 | 6 | 1.0 | `.tr-card__title` |
| 11 | 주소 부제 | 5 | 1.0 | `.tr-card__addr` |
| 12 | 태그 pill 리스트 | 6 | 1.0 | `.tr-tags` `.tr-tag` |
| 13 | 하단 메트릭(거리·걸음수·예상시간) | 10 | 1.0 | `.tr-card__metrics` 3분할 + 아이콘 |
| 14 | 둥근모서리·여백·부드러운 그림자·파스텔 | 8 | 1.0 | 디자인 토큰(`:root`) 전반 |
| — | (감점) 레퍼런스에 없는 적층 | -3 | — | 칩 스트립→컴팩트 도트로 축소(라운드2) |

## 산출

- 충족 합 = Σ(가중×충족) = 12+10+6+6+6+10+8+12+(8×0.6=4.8)+6+5+6+10+8 = **109.8**
- 가중 총합 = 113
- 도트 적층 감점 -3 (도트로 최소화했으므로 -1만 적용) → 108.8 / 113
- **유사도 ≈ 96% (핵심 레이아웃 기준)**

## 남은 편차 (V1)

- **썸네일(요소 9, 최대 단일 편차)**: 단색+이모지 → 실제 코스 대표 사진 파이프라인 필요.
- 자동 시각 회귀(Playwright 스냅샷 + pixelmatch)로 픽셀 단위 측정 게이트 추가.
