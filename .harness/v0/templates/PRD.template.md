# PRD — {제목}

> 생성: `/debate-loop` · slug: `{slug}` · 출처 템플릿: mattpocock `to-prd`

## Problem Statement
사용자가 겪는 문제를 사용자 관점에서.

## Solution
문제에 대한 해결책을 사용자 관점에서.

## User Stories
번호가 매겨진 긴 유저스토리 목록. 각 항목은 다음 형식:

1. As a <actor>, I want a <feature>, so that <benefit>

(기능의 모든 측면을 폭넓게 커버하도록 충분히 많이.)

## Implementation Decisions
토론에서 합의된 구현 결정. 모듈/인터페이스/아키텍처/스키마/API 계약/상호작용 등.
구체 파일 경로·코드 스니펫은 넣지 않는다(빠르게 낡으므로). 단 결정을 정확히 인코딩하는
상태머신·스키마·타입은 핵심만 인라인 가능.

## Testing Decisions
좋은 테스트의 기준(외부 동작만 테스트), 테스트 대상 모듈, 프로젝트 내 prior art.

## Out of Scope
이 PRD 범위 밖 항목.

## Further Notes
추가 메모. devil이 끝까지 남긴 미해결 리스크가 있으면 여기 명시.
